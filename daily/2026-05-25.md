# 每日学习笔记

> 2026-05-25 · 周一 · Phase 2 收尾 + Agent Wallet 实操

## 进度更新

- ✅ 第一阶段（AI 基础，11 章）：5/19 完成，5/24 复习
- ✅ 第二阶段（AI × Web3 Bridge，12 章）：5/24 全部读完
  - 第 1-2 章（Chain-aware Context / Web3 Tool Use）：详细笔记见 5/21、5/22
  - 第 3-12 章：快速阅读通过，核心章节（Agent Workflow、Agent Wallet、AI Security）理解较深

## 两阶段 Handbook 全部完成

Handbook 约 42 章全覆盖。接下来转向实践为主。

## 下一步方向

进入深度实践阶段，同时在 `handbook-feedback/` 记录阅读中发现的问题和建议。

---

## Agent Wallet 实操（Path B）

> Safe + Session Key 部署脚本 + AllowanceModule 集成设计

根据昨天的 Bridge 总结，Agent Wallet 是唯一没有具体产出的高优先级章节。今天补上。

### Safe protocol-kit 部署流程研究
- `Safe.init()` → `createSafeDeploymentTransaction()` → `connect()`
- 完整理解了 Safe 的 counterfactual 部署（还没部署就知道地址）

### AllowanceModule 源码研究
- 从 [safe-modules](https://github.com/safe-global/safe-modules) 读了 allowances 模块
- 核心概念：delegate（被授权者）+ allowance（token 级限额）+ EIP-712 签名 + nonce 防重放
- `amount` / `spent` / `resetTimeMin` / `lastResetMin` / `nonce` 五点结构

### ERC-4337 Safe 集成理解
- `Safe4337Pack` from relay-kit
- 流程：createTransaction → signSafeOperation → executeTransaction
- Paymaster 支持（ERC-20 paymaster + verifying/sponsored paymaster）

### 编写两个脚本（npm 依赖已安装，解析通过验证）
- `deploy-safe.ts`：Safe 部署 + 提案-确认模式演示
- `test-allowance.ts`：AllowanceModule 完整交互流程（含 EIP-712 签名构建）

### 关键洞察

**Safe 的权限模型本质上就是为「不信任的执行者」设计的。** AI Agent 只是这个「不信任的执行者」的另一个实例——和多签场景里的「你不完全信任的同事」在安全模型上是同构的。

这个洞察改变了技术选择：不需要为 AI Agent 重新发明钱包。Safe + AllowanceModule + ERC-4337 已经提供了完整的基础设施。

---

## 复习巩固（Path A）

遍历全部 18 份参考设计 + EIP-8004 + Safe 实操，写了 [思维导图](2026-05-25-mindmap.md)：每份设计的「核心问题 + 解决方案 + 一条限制」。

### 统一认知框架

全部设计归约到一个三层模式：

```
事前约束（Session Key / Policy / Allowance / 白名单）
    → 事中验证（Evaluator / AI Oracle / TEE / 多节点共识）
        → 事后追责（声誉 slashing / 链上审计 / kill switch / 仲裁）
            → 全部由链上强制执行（代码不可绕过）
```

### Bridge 三条主线全部验证

- **权限分层**：constrained-web3-assistant 的读/写分离 → agent-api-payment-flow 的 Session Key → agent-escrow-report-flow 的 Evaluator/Arbiter 分工
- **可验证性**：agent-profile-design 的 IPFS CID → reputation 的链上 event → ai-oracle 的 TEE 证明 → inference-network 的多节点共识
- **人始终有最终控制权**：sovereignty 的 kill switch → escrow 的 24h 挑战窗口 + 仲裁 → safe-session-key 的 removeDelegate() 不需要 delegate 同意

---

## 深度知识：Agent 架构模式

> 补充 Handbook 没覆盖的工程层面知识

### 决策循环三模式

| 模式 | 一句话 | Web3 类比 |
|------|--------|-----------|
| **ReAct** | 想一步，做一步，看一眼，再想一步 | 手动盯盘，边看边交易 |
| **Plan-Execute** | 先画完整地图，再逐步执行 | TWAP 定投，定好计划自动跑 |
| **Multi-Agent** | 多个 Agent 分头干活，统一汇报 | DAO 工作组分工 |

- ReAct 是 Hermes 的底层模式（Thought → Action → Observation 循环）
- 死循环的根源：Agent 无法区分"没查到"和"方法不对"
- 解决：设最大步数、要求 Agent 换思路、工具返回加元数据

### Multi-Agent 深入

**任务分配**：
- Centralized（Orchestrator 说了算）→ 实用，90% 的系统用这个
- Decentralized（Agent 自己商量）→ 灵活但协调成本高

**拆分维度**：
- 按领域：Solidity 写手 / 前端写手 / 审计 → 适合并行任务
- 按阶段：研究 → 策略 → 执行 → 验证 → 适合流水线任务

**安全保障（三道防线）**：
1. Orchestrator 做 sanity check
2. 独立 Reviewer Agent 审查
3. 涉及钱的留给人类确认

**经验法则**：能用一个 Agent 搞定的，不要用多个。

---

## 产出

- `experiments/safe-session-key/` — 完整项目
  - `src/deploy-safe.ts` — Safe 部署脚本
  - `src/test-allowance.ts` — AllowanceModule 集成测试（概念 + 实际代码）  
  - `NOTES.md` — 学习笔记 + Bridge 关联
  - `package.json` / `tsconfig.json`
- `daily/2026-05-25-mindmap.md` — 19 份产出思维导图（核心问题 + 解决方案 + 限制）

---

## 明天

- 在 fork 上实际跑通 deploy-safe.ts（需要启动 Anvil）
- 或：深入 AllowanceModule 的 EIP-712 签名构建，在 fork 上实测 executeAllowanceTransfer
